export interface SlideHistoryEntry {
  id: string;
  headline: string;
  visualDescription: string;
  category: string;
}

export interface StyleReference {
  headline: string;
  visualDescription: string;
  category: string;
  slideNumber: number;
}

